{
  "name": "jose-worker",
  "version": "0.0.0",
  "devDependencies": {
    "jest": "^29.7.0",
    "wrangler": "3.13.2"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "jest",
    "build": "cp index.js dist/index.js"
  },
  "dependencies": {
    "google-auth-library": "^9.1.0"
  }
}
